# prompt = input("Please enter a sentense: ")
# print(prompt.find('a'))

prompt = input("enter a number: ")
temp = float(prompt)
# rounded_num = round(temp,2)

# print(f"{temp} rounded to 2 decimal places is: {rounded_num}")

print(f"{prompt} rounded to 2 decimal places is: {round(temp,2)}")

# abs_value = float(prompt)

# print(f"Abaolute value of {prompt} is: {abs(abs_value)}")